# Convex deployment URL for the frontend (get it from `npx convex dev` or the Convex dashboard)
VITE_CONVEX_URL=https://your-deployment.convex.cloud

# Optional: enable read-only demo/guest mode with sample data
# VITE_GUEST_MODE=true
