# Convex
VITE_CONVEX_URL=https://your-project.convex.cloud

# Note: Authentication is handled by Convex Auth with Email/Password
# The following environment variables are set directly in Convex:
# - SITE_URL (Your application URL)
# - JWT_PRIVATE_KEY (PEM formatted private key)
# - JWKS (JSON Web Key Set)
#
# Set these using: bunx convex env set VARIABLE_NAME "value"
